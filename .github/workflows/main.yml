sudo tee /etc/apt/sources.list.d/ngrok.list \
	&& sudo apt update \sudo tee /etc/apt/sources.list.d/ngrok.list \
	&& sudo apt update \sudo tee /etc/apt/sources.list.d/ngrok.list \
	&& sudo apt update \sudo tee /etc/apt/sources.list.d/ngrok.list \
	&& sudo apt update \sudo tee /etc/apt/sources.list.d/ngrok.list \
	&& sudo apt update \sudo tee /etc/apt/sources.list.d/ngrok.list \
	&& sudo apt update \
